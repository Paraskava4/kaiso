import { CustomReportsPage } from '@/page-components';

/**
 * Custom Reports Route Component
 *
 * This is the Next.js pages router page component for the custom-reports route (/custom-reports).
 */
export default function CustomReports() {
  return <CustomReportsPage />
}